{% extends "base.html.twig" %}

{% block title %}{{ '%entity% edit'|trans({'%entity%': 'Cinéma'}, 'admin') }} - {{ parent() }}{% endblock %}

{% block body %}
    <section class="content">
        <div class="row">
            <div class="col-xs-12">
                <div class="box box-primary">
                    <div class="box-header">
                        <h1 class="box-title">{{ '%entity% creation'|trans({'%entity%': 'Cinéma'}, 'admin') }}</h1>
                    </div>
                    <div class="box-body">
                        {{ form_start(edit_form) }}
                        <div class="form-group">
                            <label for="state_nom">Nom</label>
                            {{ form_widget( edit_form.title, { "attr": { "class": "form-control" }}) }}
                            {{ form_errors( edit_form.title ) }}
                        </div>
                        <div class="form-group">
                            <label for="state_nom">Prix (en F CFA)</label>
                            {{ form_widget( edit_form.price, { "attr": { "class": "form-control" }}) }}
                            {{ form_errors( edit_form.price ) }}
                        </div>
                        <div class="form-group">
                            <label for="state_nom">Localisation</label>
                            {{ form_widget( edit_form.location, { "attr": { "class": "form-control" }}) }}
                            {{ form_errors( edit_form.location ) }}
                        </div>
                        {{ form_widget(edit_form) }}
                        <button class="btn btn-primary pull-left" type="submit"><i class="fa fa-edit"></i> {{ 'Edit'|trans({}, 'admin') }}</button>
                        {{ form_end(edit_form) }}

                        {{ form_start(disapprove_form) }}
                        {{ form_widget(disapprove_form) }}
                        <div class="btn-group record_actions pull-right">
                            <a class="btn btn-info" href="{{ path('admin_place') }}">
                                <i class="fa fa-list"></i> {{ 'Back to the list'|trans({}, 'admin') }}
                            </a>
                            <button class="btn btn-danger" type="submit"><i class="fa fa-trash-o"></i> {{ 'Delete'|trans({}, 'admin') }}</button>
                        </div>

                        <input name="modal" type="hidden" value="0">
                        {{ form_end(disapprove_form) }}
                    </div>
                </div>
            </div>
        </div>
    </section>

    <div id="delete_confirm" class="modal fade">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                    <h3>{{ 'Confirm delete'|trans({}, 'admin') }}</h3>
                </div>
                <div class="modal-body">
                    <p>{{ 'You are about to delete an item'|trans({}, 'admin') }}, {{ 'this procedure is irreversible'|trans({}, 'admin') }}.</p>
                    <p>{{ 'Do you want to proceed?'|trans({}, 'admin') }}</p>
                </div>
                <div class="modal-footer">
                    <button class="btn btn-danger">{{ 'Yes'|trans({}, 'admin') }}</button>
                    <button class="btn btn-default" data-dismiss="modal">{{ 'No'|trans({}, 'admin') }}</button>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
